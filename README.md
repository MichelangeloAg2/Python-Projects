# Python-Bookstore
For data insertion, I used Python libraries for connections to MySQL (specifically, I used MariaDB as the database), setting up data insertion via the input method via the dedicated web page.
